<?xml version="1.0" encoding="UTF-8"?>
<!--
  Transporte stdio: o STDOUT precisa conter APENAS o protocolo JSON-RPC do MCP.
  Por isso o log vai para STDERR (que o Claude Desktop captura no proprio log e nao
  interfere no protocolo). Nao usamos arquivo para evitar problemas de permissao/
  diretorio de trabalho quando o Claude Desktop lanca o processo.
-->
<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDERR"/>
    </root>

    <logger name="com.millenium" level="INFO"/>
</configuration>
